@model VzOverFlow.Models.ViewModels.CreateReportViewModel
@{
 ViewData["Title"] = "Báo cáo vi phạm";
}

<div class="max-w-2xl mx-auto py-8 font-sans">
<div class="bg-white border border-slate-200 rounded-lg p-6 shadow-sm">
    <h1 class="text-2xl font-semibold text-slate-900 mb-2">🚨 Báo cáo nội dung vi phạm</h1>
      <p class="text-sm text-slate-600 mb-6">
            Giúp chúng tôi xây dựng cộng đồng tốt đẹp hơn bằng cách báo cáo nội dung vi phạm quy tắc.
 </p>

        @if (!string.IsNullOrEmpty(Model.ContentType))
  {
 <div class="bg-blue-50 border border-blue-200 rounded-lg p-4 mb-6">
        <p class="text-xs text-blue-800 mb-1 font-medium"><strong>Loại nội dung:</strong> @Model.ContentType</p>
      <p class="text-xs text-blue-700 break-words">
     <strong>Nội dung:</strong> @Model.ContentPreview
     </p>
    </div>
}

        <form asp-action="Create" method="post" class="space-y-6">
            @Html.AntiForgeryToken()
 <input type="hidden" asp-for="QuestionId" />
   <input type="hidden" asp-for="AnswerId" />

            <div>
         <label class="block text-sm font-medium text-slate-700 mb-2">
   Lý do báo cáo <span class="text-red-500">*</span>
      </label>
 <select asp-for="Reason" 
 class="w-full rounded-md border border-slate-300 px-3 py-2 text-sm font-sans focus:outline-none focus:ring-2 focus:ring-red-500">
      <option value="">-- Chọn lý do --</option>
    <option value="1">🚫 Spam hoặc quảng cáo</option>
           <option value="2">⚠️ Nội dung không phù hợp</option>
   <option value="3">😡 Ngôn từ thù địch hoặc lăng mạ</option>
            <option value="4">❌ Thông tin sai lệch</option>
       <option value="5">📄 Vi phạm bản quyền</option>
      <option value="6">🔄 Nội dung trùng lặp</option>
       <option value="99">📝 Lý do khác</option>
   </select>
            <span asp-validation-for="Reason" class="text-xs text-red-500"></span>
   </div>

      <div>
  <label class="block text-sm font-medium text-slate-700 mb-2">
    Chi tiết bổ sung (tùy chọn)
                </label>
        <textarea asp-for="Details" 
       rows="5"
         placeholder="Mô tả chi tiết về vi phạm để giúp chúng tôi xem xét chính xác hơn..."
    class="w-full rounded-md border border-slate-300 px-3 py-2 text-sm font-sans focus:outline-none focus:ring-2 focus:ring-red-500"></textarea>
    <span asp-validation-for="Details" class="text-xs text-red-500"></span>
            <p class="text-xs text-slate-500 mt-1">Tối đa 1000 ký tự</p>
  </div>

  <div class="bg-yellow-50 border border-yellow-200 rounded-lg p-4">
       <h3 class="text-sm font-semibold text-yellow-900 mb-2">⚠️ Lưu ý quan trọng</h3>
    <ul class="text-xs text-yellow-800 space-y-1 ml-4 list-disc">
  <li>Báo cáo sai sự thật có thể dẫn đến hạn chế tài khoản</li>
         <li>Chúng tôi sẽ xem xét và phản hồi trong vòng 24-48 giờ</li>
<li>Mọi báo cáo đều được bảo mật thông tin người báo cáo</li>
     </ul>
  </div>

  <div class="flex gap-3">
                <button type="submit" 
          class="flex-1 rounded-md bg-red-600 text-white px-4 py-2 hover:bg-red-700 font-medium">
               📤 Gửi báo cáo
     </button>
       <a href="javascript:history.back()" 
             class="flex-1 text-center rounded-md border border-slate-300 bg-white text-slate-700 px-4 py-2 hover:bg-slate-50 font-medium">
   Hủy bỏ
        </a>
   </div>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}